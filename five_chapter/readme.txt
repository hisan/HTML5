第五章 初探JavaScript 2020-05-09 杭州

一：重要概念
	1.脚本是HTML文档的一部分

二：HTML中JS语言与元素的结合
	0.创建对象:使用new Object()或对象字面量语法
	
	1.给对象添加方法：新建一个属性,然后将一个函数赋给他。
	
	2.获取或设置对象的属性值：使用圆点或数组索引符号表示法。
	
	3.枚举对象中的属性：for...in
	
	4.给对象添加属性或方法：将一个值赋给所需属性名
	
	5.删除对象的一个属性：delete关键字
	
	6.判断一个对象是否有某个属性：使用in表达式
	
	7.判断两个对象的值是否相等（不考虑类型差别）：==
	
	8.判断两个对象的类型和值是否相等（不考虑类型差别）：===
	
	9.显示类型转换：使用Number或String函数
	
	10.创建数组：使用 new Array()或数组字面量
	
	11.读写数组内容：
		11.1 使用索引符号表示法获取数组中指定位置元素的值
		11.2 将新值赋给它
	
	12.枚举数组的内容：使用for循环
	
	13.处理错误：使用try...catch
	
	14.比较null和undefined值：
		将值转换为boolbean类型，或在需要同等对待null和undefined时使用相等运算符==，在要区别对待它们时使用恒等运算符===

三：脚本使用方式：
	1.内嵌脚本
	
	2.外部脚本

四：

五：

六：

七：